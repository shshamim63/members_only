<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <% if @user.logged_in? %>
          <td><%= post.user_id.user.name %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if @user.logged_in? %>
  <%= link_to 'New Post', new_post_path, class: "btn btn-lg btn-primary" %>
<% end %>
